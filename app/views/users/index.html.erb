<% provide(:title, t("static_pages.all_users")) %>
<h1><%= t("static_pages.all_users") %></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
